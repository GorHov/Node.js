<h1>Edit {{course.title}}</h1>

<form action="/courses/edit" method="POST" class="course-form">
    <div class="input-field col s6">
        <input id="title" name="title" type="text" class="validate" required value="{{course.title}}">
        <label for="title">Course Name</label>
        <span class="helper-text" data-error="Add Name"></span>

    </div>

    <div class="input-field col s6">
        <input id="price" name="price" type="text" class="validate" required value="{{course.price}}">
        <label for="price">Course Price</label>
        <span class="helper-text" data-error="Add price"></span>
    </div>

    <div class="input-field col s6">
        <input id="img" name="img" type="text" class="validate" required min="2" value="{{course.img}}">
        <label for="img">Picture URL</label>
        <span class="helper-text" data-error="Add URL"></span>
    </div>

    <input type="hidden" name="id" value="{{course.id}}">

    <button type="submit" class="btn btn-primary">Edit Course</button>
</form>
<form action="/courses/remove" method="POST">
    <input type="hidden" name="id" value="{{ course.id }}">
    <button class="btn red">Delete Course</button>
</form>